//while (condition) {execution}

let index = 0;
while(index<=6)
    {
        // console.log(index);
        //to terminate the loop the increment and decrement is must
        index++
    }

let indexDec = 10;
while(indexDec >6)
    {
        // console.log(indexDec);
        //to terminate the loop the increment and decrement is must
        indexDec--
    }

    //do while
    let doVar=5

    do{
        //alwys execute this block
        console.log(doVar);
        doVar++;
    }while(doVar<=4)//false but the do will execute that code